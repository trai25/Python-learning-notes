# Python 里创建一个变量的方法很简单，给它起个名字，然后用 = 赋予他一个值就行了。
# = 的左边是变量名，右边是值。

# 例子：
# name    = 'trai25'	字符串 - 表示遗传字符，需要用''或""引起来；
# myVar   = 25			整数
# price   = 5.99		浮点数 - 就是小数
# visible = True		bool(布尔) - 这个比较特殊，是用来表示逻辑「是」「非」的一种类型，它只有两个值：True 和 False。(这里没有引号，有了引号就变成字符串了)

